# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overridden

# append a local nodejs installation to the $PATH if it exists
if [ -s "${HOME}/.nodejs/bin" ]; then
  export PATH="${HOME}/.nodejs/bin:$PATH"
fi
